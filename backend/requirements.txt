fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg[binary]==3.2.2
pgvector==0.3.4
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pinned: passlib 1.7.4 (unmaintained since 2020) probes bcrypt's internal
# __about__.__version__ attribute to detect its version; bcrypt>=4.1 removed
# that attribute, which makes passlib's own self-test crash with a
# "password cannot be longer than 72 bytes" ValueError on every hash/verify
# call, regardless of the actual password. Pin bcrypt below that break.
bcrypt==4.0.1
python-multipart==0.0.9
langchain==0.3.3
langchain-openai==0.2.2
openai==1.51.2
celery==5.4.0
redis==5.0.8
boto3==1.35.36
cloudinary==1.41.0
twilio==9.3.0
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
